Builds a balanced and complete binary search tree.

Prompts user to enter # of nodes/elements to add to a binary search tree. Will build a BST that is as balanced and as complete as possible given the # of nodes decided upon. Will also output the node # of the root node to help check if tree is properly balanced.